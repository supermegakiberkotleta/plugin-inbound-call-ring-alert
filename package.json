{
  "name": "plugin-inbound-call-ring-alert",
  "version": "0.0.18",
  "private": true,
  "scripts": {
    "preinstall": "npx flex-plugin pre-script-check",
    "start": "twilio flex:plugins:start",
    "build": "twilio flex:plugins:build",
    "deploy": "twilio flex:plugins:deploy",
    "release": "twilio flex:plugins:release",
    "test": "twilio flex:plugins:test"
  },
  "dependencies": {
    "@twilio/flex-plugin": "^7.1.2",
    "react": "16.13.1",
    "react-dom": "16.13.1"
  },
  "devDependencies": {
    "@twilio/flex-plugin-scripts": "7.1.2",
    "@twilio/flex-ui": "^1",
    "react-test-renderer": "16.13.1"
  }
}